#ifndef APP_VALIDATION_H
#define APP_VALIDATION_H

#include <iostream>
#include <string.h>

bool isSendCommand (std::string value);
bool isReadCommand (std::string value);
bool isRemoveCommand (std::string value);

#endif